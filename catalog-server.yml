server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  application:
    name: catalog-server
datasource:
  driverClassName: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://localhost:3370/catalog-dev
  username: user-catalog
  password: pwd-catalog
jpa:
  hibernate.ddl-auto: update
  generate-ddl: true
  show-sql: true

